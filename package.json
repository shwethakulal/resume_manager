{
  "name": "resume_manager",
  "version": "1.0.0",
  "description": "To maintain the users resumes.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shwethakulal/resume_manager.git"
  },
  "author": "Shwetha",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/shwethakulal/resume_manager/issues"
  },
  "homepage": "https://github.com/shwethakulal/resume_manager#readme",
  "dependencies": {
    "mongoose": "^5.0.0-rc0",
    "morgan": "^1.8.1",
    "restify": "^6.3.4"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-es2015": "^6.24.0"
  }
}
